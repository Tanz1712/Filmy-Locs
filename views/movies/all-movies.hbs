
<br /> <br> <br>

<h3> <span style='font-size:50px;'> &#127916;</span>List of all movies</h3>

<hr />

{{! since we passed allTheMoviesFromDB as `movies`,
we can iterate over it here with #each}}

<div>
{{#each movies}}

  <div class="movie-card">

    <h2><span style="font-size:26px; color:black;">Title:</span> {{this.title}} </h2> <br>

    <img class="movie-img" src="{{this.imageUrl}}" alt="{{this.title}}" />

    <br /> <br>
  </div>


   <div>
   
    {{! link that points to the details page }}
    <a href="/userProfile/movies/{{this._id}}">See movie details</a> <span>|</span>
   
    {{! link to redirect to the edit form for each specific movie  }}
    <a href="/userProfile/movies/{{this._id}}/edit">Edit</a>


  {{! add the form to handle the POST request to remove a specific movie  }}
    <form action="/userProfile/movies/{{this._id}}/delete" method="POST">
      <button>Delete</button>
    </form>
  
  </div>
  
    <hr />
  </div>

<!--  <h1>Movies</h1>
<div>
  {{#each movies}}
    <div class="card" style="width: 18rem;">
      <img src="{{this.imageUrl}}" class="card-img-top" alt="{{this.title}}" />
      <div class="card-body">
        <h5 class="card-title">{{this.title}}</h5>
      </div>
      <div class="card-body">
        <a href="/userProfile/movies/{{this._id}}" class="card-link">See More</a>
      </div>
    </div>
  {{/each}}
</div>-->

{{else}}

  <p>There are no movies yet!</p>

{{/each}}