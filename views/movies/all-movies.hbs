<br />
<h4>List of all movies</h4>

<hr />

{{!-- since we passed allTheMoviesFromDB as `movies`,
we can iterate over it here with #each--}}

{{#each movies}}

  <div class="movie-card">

    <h5>Name: {{name}}</h5>

    <img class="movie-img" src="{{imageUrl}}" alt="{{name}}" />

    <br />

    <a href="/userProfile/movies/{{_id}}">See movie details</a> {{!-- link that points to the details page --}}
    <a href="/userProfile/movies/{{_id}}/edit">Edit</a> {{!-- link to redirect to the edit form for each specific book  --}}

     {{!-- add the form to handle the POST request to remove a specific book  --}}
    <form action="/userProfile/movies/{{_id}}/delete" method="POST">
      <button>Delete</button>
    </form>

    <hr />
 

{{else}}

  <p>There are no movies yet!</p>

 </div>

{{/each}}